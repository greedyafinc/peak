{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Peak main window.",
  "windows": ["main"],
  "permissions": ["core:default", "store:default"]
}
